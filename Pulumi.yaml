name: pulumaws
description: project to help the deployment of a Docker swarm inside AWS
runtime:
  name: python
  options:
    toolchain: pip
    virtualenv: .venv
config:
  pulumi:tags:
    value:
      pulumi:template: aws-python
